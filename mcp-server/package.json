{
  "name": "geoagent-mcp-server",
  "version": "0.1.0",
  "description": "Standalone Node.js MCP server exposing a campus geocode tool over the Model Context Protocol (stdio transport). Part of the GeoAgent upgrade's optional 'extension' scope (outline section 11) — demonstrates the Python Orchestrator calling a tool through the MCP protocol instead of an in-process function call.",
  "type": "module",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "node test/test-client.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
